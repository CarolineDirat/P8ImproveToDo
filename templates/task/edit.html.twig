{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-12">
            {{ form_start(form, {'action' : path('task_edit', { 'id' : task.id })}) }}
                {{ form_widget(form) }}

                <button type="submit" class="btn btn-success pull-right">Modifier</button>
            {{ form_end(form) }}

            <a href="{{ path('task_list_all') }}" class="btn btn-primary btn-sm mt-2">Retour à la liste des tâches</a>
        </div>
    </div>
{% endblock %}
