<form action="{{ path('task_delete', {'id': task.id }) }}" method="DELETE">
    <input type="hidden" name="token" value="{{ csrf_token('delete-task-'~ task.id) }}"/>
    <button id="delete-task-{{ task.id }}" class="btn btn-danger btn-sm mt-1">Supprimer</button>
</form>
